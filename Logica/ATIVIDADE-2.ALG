Algoritmo "Atividade-2"

Var
contador, numeroDaTabuada : inteiro


Inicio
escreval("insira a tabuada desejada")
leia(numeroDaTabuada)

contador <- 1
enquanto (contador <= 10) faca

escreval(numeroDaTabuada, " X ", contador," = ", contador * numeroDaTabuada)

contador <- contador + 1
fimEnquanto

Fimalgoritmo